package com.example.snake;

import android.content.Context;
import android.graphics.Point;

public class SnakeNormal extends Snake {
    public SnakeNormal(Context context, Point mr, int ss) {
        super(context, mr, ss);
    }
}
